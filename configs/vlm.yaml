vlm:
  provider: local
  model_name_or_path: Qwen/Qwen3-VL-2B-Instruct
  device: auto
  dtype: auto
  max_new_tokens: 512
  temperature: 0.0
  timeout_seconds: 60
  max_retries: 2
  image_max_side: 1008
  structured_output: true
  prompt_version: v1
  max_detections: 20
  max_ocr_blocks: 50
  max_ocr_chars: 4000
  warmup_enabled: false
  cache_dir: ../artifacts/huggingface_cache
  prompts_dir: ../prompts/vlm
  artifacts_dir: ../artifacts/vlm
  experiment_name: qwen3_vl_2b_v1
